import { Config } from '../types/ws';
/**
 * 验证配置对象的有效性
 * @param config 要验证的配置对象
 * @returns 是否有效
 */
export declare function validateConfig(config: Partial<Config>): config is Config;
//# sourceMappingURL=validate.d.ts.map